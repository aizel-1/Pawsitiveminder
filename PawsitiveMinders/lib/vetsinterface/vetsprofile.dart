import 'package:flutter/material.dart';

class Vetsprofile extends StatelessWidget {
  const Vetsprofile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}