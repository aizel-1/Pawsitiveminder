import 'package:flutter/material.dart';

class VetsAppointment extends StatelessWidget {
  const VetsAppointment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}